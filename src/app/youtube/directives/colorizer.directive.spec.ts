import { ColorizerByDateDirective } from './colorizer.directive';

describe('ColorizerByDateDirective', () => {
  it('should create an instance', () => {
    const directive: ColorizerByDateDirective = new ColorizerByDateDirective(null, null);
    expect(directive).toBeTruthy();
  });
});
