import { ChangeSortingOrderDirective } from './change-sorting-order.directive';

describe('ChangeSortingOrderDirective', () => {
  it('should create an instance', () => {
    const directive: ChangeSortingOrderDirective = new ChangeSortingOrderDirective();
    expect(directive).toBeTruthy();
  });
});
