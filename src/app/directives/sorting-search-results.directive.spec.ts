import { SortingSearchResultsDirective } from './sorting-search-results.directive';

describe('SortingSearchResultsDirective', () => {
  it('should create an instance', () => {
    const directive = new SortingSearchResultsDirective();
    expect(directive).toBeTruthy();
  });
});
